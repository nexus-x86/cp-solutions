#include <iostream>

using namespace std;

int main() {
    int N, T;
    cin >> N >> T;
    // N Cups from 1 to N filled up T times
    // for each filling, c chocolates are added to all cups between cup a and b inclusive

    // special someone can be given more than 1 cup, and the cups must be numbered in order
    // special someone can be given a max of L chocolates
}